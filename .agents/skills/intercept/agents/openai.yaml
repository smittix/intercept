interface:
  display_name: "Intercept"
  short_description: "Repo-specific patterns and workflows for intercept"
  default_prompt: "Use the intercept repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true